# Test evidence

Machine-generated JUnit XML and compact manifests from acceptance runs are
stored here. A passing file is committed only when the corresponding command
was actually executed; failure output is retained as failure evidence.

