# Results

Small, immutable evidence and summaries may be committed here. Resumable raw
runs live in `results/runs/` and are ignored because real-data per-query output
can be large. Every reported number must identify its raw run ID and hashes.

