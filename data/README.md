# Data directory

Downloaded and processed vectors are intentionally untracked. Use
`make data`; provenance, terms links, cryptographic checksums, and extraction
IDs are written to manifests. Never commit the large files.

